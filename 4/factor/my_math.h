#include <stdbool.h>

/*
    We have two main functions
    
    @print_factor-prints all the possible deviders of a number
    @isPrime-checks if a number is prime
*/

void print_factor(int num);
bool isPrime();
